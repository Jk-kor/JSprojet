<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= logement.name %> - Détails</title>
    <link rel="stylesheet" href="/css/detail_style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>
<body>
    <header>
        <div class="header-container">
            <div class="logo">
                <a href="/"><i class="fas fa-home"></i> airair</a>
            </div>
            <nav>
                <ul>
                    <li><a href="/">Accueil</a></li>
                </ul>
            </nav>
        </div>
    </header>

    <main>
        <div class="container">
            <div class="logement-detail">
                <div class="image-gallery">
                    <img src="/images/<%= logement.photos[0] %>" alt="<%= logement.name %>" class="main-image">
                </div>
                
                <div class="logement-info">
                    <h1><%= logement.name %></h1>
                    <div class="rating">
                        <% for(let i = 0; i < Math.floor(logement.note); i++) { %>
                            <i class="fas fa-star"></i>
                        <% } %>
                        <% if(logement.note % 1 !== 0) { %>
                            <i class="fas fa-star-half-alt"></i>
                        <% } %>
                        <span><%= logement.note %>/5</span>
                    </div>
                    
                    <div class="price-tag">
                        <span class="price"><%= logement.prix %>€</span> / nuit
                    </div>
                    
                    <div class="description">
                        <h2>Description du logement</h2>
                        <p><%= logement.description %></p>
                    </div>
                    
                    <div class="details">
                        <div class="detail-item">
                            <i class="fas fa-users"></i>
                            <span>Capacité: <%= logement.capacite || '2' %> personnes</span>
                        </div>
                    </div>
                    
                    <div class="booking-section">
                        <button class="book-btn">Réserver maintenant</button>
                    </div>
                    
                    <a href="/" class="back-link"><i class="fas fa-arrow-left"></i> Retour à la liste</a>
                </div>
            </div>
        </div>
    </main>

    <footer>
        <div class="footer-container">
            <p>&copy; 2025 JinQ & Bapt!ste</p>
            <div class="social-links">
                <a href="#"><i class="fab fa-facebook"></i></a>
                <a href="#"><i class="fab fa-instagram"></i></a>
                <a href="#"><i class="fab fa-twitter"></i></a>
            </div>
        </div>
    </footer>
</body>
</html>